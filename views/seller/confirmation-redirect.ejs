<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Zetu Cart - Email Confirmation</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/tailwindcss/2.2.19/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-orange-100 to-orange-200 min-h-screen flex justify-center items-center">

    <div class="bg-white p-8 rounded-2xl shadow-xl max-w-md w-full text-center transform transition duration-300 ease-in-out scale-95 hover:scale-100">
        <img src="/public/images/zetu-logo.jpeg" alt="Company Logo" class="mx-auto mb-6 h-16 w-auto opacity-90">

        <h1 class="text-3xl font-extrabold text-orange-700 mb-4 animate-fadeIn">🎉 Email Confirmed!</h1>
        <p class="text-gray-700 text-lg mb-4">Hello <%= businessName %>, your email has been successfully registered.</p>
        <p class="text-gray-700 mb-6">Let's get down to business 🤝</p>
        
        <p class="text-gray-600 text-sm italic opacity-90 animate-fadeInSlow">Redirecting to login page shortly...</p>

        <!-- Spinner Animation -->
        <div class="mt-6 flex justify-center">
            <div class="w-10 h-10 border-4 border-orange-300 border-t-orange-600 rounded-full animate-spin"></div>
        </div>
    </div>

    <script>
        setTimeout(() => {
            window.location.href = '/seller/login';
        }, 5000);
    </script>

    <style>
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(-10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        @keyframes fadeInSlow {
            from { opacity: 0; }
            to { opacity: 0.9; }
        }
        .animate-fadeIn { animation: fadeIn 0.8s ease-in-out; }
        .animate-fadeInSlow { animation: fadeInSlow 2s ease-in-out; }
    </style>
</body>
</html>
