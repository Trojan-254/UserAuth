<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }

        body {
            display: flex;
            min-height: 100vh;
            background: #f4f4f9;
            color: #333;
        }

        /* Sidebar Styling */
        .sidebar {
            width: 250px;
            background: #6c63ff;
            color: #fff;
            display: flex;
            flex-direction: column;
            padding: 1rem;
        }

        .sidebar h2 {
            text-align: center;
            margin-bottom: 1.5rem;
            font-size: 1.8rem;
        }

        .sidebar a {
            text-decoration: none;
            color: #fff;
            display: block;
            padding: 0.8rem;
            border-radius: 5px;
            margin: 0.3rem 0;
            transition: background 0.3s ease;
        }

        .sidebar a:hover {
            background: #5a55d6;
        }

        /* Main Content */
        .main-content {
            flex: 1;
            padding: 2rem;
            display: flex;
            flex-direction: column;
        }

        .welcome-section {
            background: #fff;
            padding: 1.5rem;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            margin-bottom: 1.5rem;
        }

        .welcome-section h1 {
            font-size: 2rem;
            color: #6c63ff;
        }

        .section-title {
            margin: 1rem 0;
            font-size: 1.5rem;
            color: #444;
        }

        .cards {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 1.5rem;
        }

        .card {
            background: #fff;
            padding: 1rem;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
            text-align: center;
            transition: transform 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
        }

        .card h3 {
            margin-bottom: 1rem;
            color: #6c63ff;
            font-size: 1.3rem;
        }

        .card p {
            color: #666;
            font-size: 0.9rem;
        }

        /* Footer */
        .footer {
            text-align: center;
            margin-top: auto;
            font-size: 0.9rem;
            color: #777;
        }
    </style>
</head>
<body>
    <!-- Sidebar -->
    <div class="sidebar">
        <h2>Dashboard</h2>
        <a href="#">Home</a>
        <a href="#">Profile</a>
        <a href="#">Orders</a>
        <a href="#">Settings</a>
        <a href="#">Help</a>
        <a href="#">Logout</a>
    </div>

    <!-- Main Content -->
    <div class="main-content">
        <!-- Welcome Section -->
        <div class="welcome-section">
            <h1>Welcome, <%= username %>!</h1>
            <p>Explore your dashboard to manage your account, view orders, update settings, and much more.</p>
        </div>

        <!-- Cards Section -->
        <h2 class="section-title">What would you like to do?</h2>
        <div class="cards">
            <div class="card">
                <h3>View Profile</h3>
                <p>Update your personal details and preferences.</p>
            </div>
            <div class="card">
                <h3>Orders</h3>
                <p>Track your recent orders and purchases.</p>
            </div>
            <div class="card">
                <h3>Settings</h3>
                <p>Manage account settings and privacy options.</p>
            </div>
            <div class="card">
                <h3>Help & Support</h3>
                <p>Get assistance and answers to common queries.</p>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            <p>&copy; 2024 Vametech. All rights reserved.</p>
        </div>
    </div>
</body>
</html>
